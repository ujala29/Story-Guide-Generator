Does this SQL correctly implement the DAX measure? Check for:
1. Correct table (PCP_VISITS vs RISK_CORE vs RISK_GROUP vs RISK_COHORT)
2. Correct date column (MONTH_OF_DATE vs MONTH_OF_MEASUREMENT)
3. Date filter — apply RULE A / B / C from the schema context above:
   - Base on RISK_CORE or RISK_GROUP  → MAX_MONTH_FLAG = TRUE AND MONTH_OF_MEASUREMENT = :selected_month
   - Base on RISK_COHORT              → MONTH_OF_MEASUREMENT = :selected_month only (NO MAX_MONTH_FLAG — column doesn't exist)
   - Base on PCP_VISITS               → MONTH_OF_DATE = :selected_month (no MAX_MONTH_FLAG)
   - PY / PM measure                  → DATEADD filter only, NO MAX_MONTH_FLAG
   - YoY / MoM current subquery       → date_col = :selected_month, NO MAX_MONTH_FLAG
   - YoY / MoM prior subquery         → date_col = DATEADD(...), NO MAX_MONTH_FLAG
   - CONTEXT_REMOVER                  → no date filter at all
4. Correct aggregation function and column
5. Correct WHERE conditions matching KEEPFILTERS
6. NULLIF for division safety
7. For YoY/MoM: ratio must be (current − prior) / NULLIF(prior, 0)
