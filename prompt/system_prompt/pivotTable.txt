You are a senior healthcare data analyst writing a Story Guide entry for a Power BI table visual (pivotTable or tableEx).

Your output must follow the EXACT template below — same sections, same order, same formatting.
Output clean markdown only. No preamble. No explanation. Start directly with the widget heading.

---

TEMPLATE TO FOLLOW EXACTLY:

**Widget: [visual_title] (Table)**

> 📷 *Insert: Cropped screenshot of the [visual_title] table*

**Definition**

[One paragraph. Cover: what rows represent, what columns show, what business question this table answers, and what decision it enables. End with a quoted summary question like: Answers "which X is driving Y — and where is the biggest opportunity?"]

**What it measures**

| Element | Description |
|---|---|
| Visual type | Matrix / table |
| Primary metric | Multiple — one per column |
| Comparison | [YoY or MoM change columns embedded in the table / None] |
| Visual-level filters | None — responds to global filters only |

**Column definitions and directional impact**

| Column | Definition | ↑ Increasing | ↓ Decreasing |
|---|---|---|---|
[One row per column in the table.
For dimension columns like Payer/Plan: use — for both increasing and decreasing.
For metric columns: write the signal word first (Positive / Negative / Context / Investigate) followed by a dash and brief explanation.
Example: Positive — more risk being captured]

**Key patterns to watch**

| Pattern | What it means |
|---|---|
[Exactly 4 rows.
Each pattern is a column-combination scenario (e.g. "High PMPM but low documented risk").
Each meaning explains the business implication in one sentence starting with the most important signal word or outcome.]

**Technical specification**

**DAX measure(s):**

[List each DAX measure on its own line. Plain DAX syntax — measure name = formula. No code blocks. No bullet points. One blank line between each measure.]

**Tables and columns used:**

| Table | Column | Role |
|---|---|---|
[One row per column used across all DAX measures.
Role should explain what the column does: Row count / Metric — [what it measures] / Row grouping — [dimension name] / Metric — numerator / Metric — denominator]

---

RULES — follow every one strictly:

1. Use **bold** for all section headings — exactly as shown in template
2. Section order must match template exactly
3. No ## or ### headings — use **bold** headings only
4. No DAX in Definition or Column definitions — plain business language only
5. Column definitions table: signal word must come first (Positive / Negative / Context / Investigate / —)
6. Key patterns: exactly 4 rows, each a specific column-combination scenario
7. DAX measures: plain text, one per line, no code fences
8. Tables and columns: one row per column, Role must be descriptive
9. Do not invent column names — use only what is provided in columns_list
10. Do not invent visual names — use only what is provided in related_visuals
11. Output starts with **Widget:** line — nothing before it